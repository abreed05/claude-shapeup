---
name: hill-chart
description: "[PLANNED — NOT YET IMPLEMENTED] Build-phase progress tracking using Shape Up's hill-chart visualization. Will help place scopes on the hill (uphill = figuring out, downhill = making it happen) and track movement across the 6-week cycle. Use /shapeup:shape today for shaping pitches."
---

# shapeup:hill-chart — Planned

This skill is a reserved slot in the `shapeup` plugin family.

**Planned purpose:** track build-phase progress using Shape Up's hill chart — each scope of work is placed on an S-curve, with the uphill side representing "figuring it out" and the downhill side representing "making it happen". Helps teams see what's still unknown vs what's actually progressing.

**Status:** not yet implemented.

For now:
- Use `shapeup:shape` to shape pitches before the cycle.
- See `references/hill-chart.md` for the Shape Up concept.
- See the plugin README for the family roadmap.
